# 채팅 발췌기

단일 HTML 파일로 동작하는 채팅 발췌 도구.

## 사용

`index.html`을 브라우저로 열면 됩니다. 빌드 과정이 없습니다.

## 구조

| 파일 | 설명 |
| --- | --- |
| `index.html` | 마크업·스타일·스크립트가 모두 들어 있는 단일 파일 |

## 수정 방법

1. `index.html`을 편집한다.
2. 브라우저에서 새로고침해 확인한다.
3. 커밋·푸시한다.

```bash
git add -A
git commit -m "설명"
git push
```
